services:
  - type: web
    name: hive-dimensions
    runtime: python
    region: oregon
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn dimensions_agent:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
